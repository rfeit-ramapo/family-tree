export enum ContextMenuType {
  NODE,
  TREE,
  RELATIONSHIP,
  CANVAS,
}

export enum ContextMenuEvent {
  RENAME = "rename",
  DELETE = "delete",
  ADD_NODE = "add-node",
  CONNECT = "connect",
  EDIT = "edit",
  VIEW = "view",
}
